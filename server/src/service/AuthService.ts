import Result from "../models/Result";

export default class ProjectService {
    public static async getList(page: number = 0, length: number = 10, sorting: any = {}): Promise<Result<any[]>> {
       
        return null;
    }
}